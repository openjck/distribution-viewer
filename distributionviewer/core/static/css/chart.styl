@import 'lib';

.axis {
  path,
  line {
    fill: none;
    stroke: $mild-blue;
    stroke-width: 2px;
  }
}
.line {
  fill: none;
  stroke-width: 1.5px;
}
// TODO: Temp. I don't want to use these colors forever.
.population-1 .line {
  stroke: $mild-blue;
}
.population-2 .line {
  stroke: red;
}
.population-3 .line {
  stroke: orange;
}
.population-4 .line {
  stroke: yellow;
}
.population-5 .line {
  stroke: green;
}
.population-6 .line {
  stroke: purple;
}
.tick {
  text {
    fill: $chart-gray;
    font-size: 11px;
    font-weight: 100;
  }
  line {
    opacity: 0.2;
  }
}
.focus {
  display: none;

  circle {
    fill: #000;
    stroke: $mild-blue;
    stroke-width: 2px;
  }
}
.hover-zone {
  fill: none;
  pointer-events: all;
}
